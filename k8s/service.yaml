apiVersion: v1
kind: Service
metadata:
  name: blue-faas
  namespace: blue-faas
  labels:
    app.kubernetes.io/name: blue-faas
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: blue-faas
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
